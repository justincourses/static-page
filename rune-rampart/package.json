{
  "name": "rune-rampart",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "npm@11.16.0",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "dev": "npm run build && wrangler pages dev ./dist",
    "deploy": "npm run build && wrangler pages deploy ./dist --project-name rune-rampart"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
